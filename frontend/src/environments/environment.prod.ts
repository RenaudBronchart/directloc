export const environment = {
  production: true,
  // In prod we recommend same-origin behind a reverse proxy (Nginx/Apache)
  // Keep it as '/api' so the browser hits the same domain.
  apiBase: '/api'
  // If you must call another domain, put 'https://api.tu-dominio.com/api'
};
